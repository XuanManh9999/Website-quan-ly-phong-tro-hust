spring:
  application:
    name: room-rental-api
  datasource:
    url: jdbc:postgresql://103.200.23.26:5432/room_rental
    driver-class-name: org.postgresql.Driver
    username: root
    password: toilamanhdevhust
  jpa:
    open-in-view: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

server:
  servlet:
    context-path: /
  error:
    include-message: always

app:
  jwt:
    # TODO: thay bằng chuỗi bí mật dài, ngẫu nhiên (HS512).
    # Lưu ý: không commit secret thật lên git nếu repo public.
    # Gợi ý: tạo chuỗi >= 64 ký tự (hoặc ~64 bytes). Ví dụ (placeholder):
    secret: REPLACE_ME_SUPER_LONG_RANDOM_SECRET_FOR_HS512_MIN_64_BYTES
    expiration-ms: 86400000
  cors:
    allowed-origins: http://localhost:5173
  vnpay:
    # TODO: điền thông tin merchant VNPay
    tmn-code: "TMJ7UWMF"
    hash-secret: "PCYPY7L159DSIEKYFRXWCAL6M503VYUH"
    payment-url: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
    return-url: http://localhost:5173/payment/return
    ipn-url: http://localhost:8080/api/v1/payments/vnpay/ipn
  gemini:
    # TODO: điền Gemini API key (server-side, không đưa ra front-end)
    api-key: ""
    model: gemini-2.5-flash
    api-url: https://generativelanguage.googleapis.com/v1beta/models
  listing:
    default-quota-per-month: 5

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.hibernate.SQL: debug
    com.hust.roomrental: info
